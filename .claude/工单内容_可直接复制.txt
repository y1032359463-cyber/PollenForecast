应用：花粉浓度播报（com.eric.PollenForecast），SDK版本：HarmonyOS SDK 6.0.0 (API 20)。

问题：地图组件无法响应系统深浅色模式切换。应用其他UI（状态栏、导航栏、页面背景）已正确适配深浅色模式，但MapComponent始终显示浅色样式。

当前实现：
1. 主题监听：在EntryAbility的onConfigurationUpdate中监听系统主题变化，并通过AppStorage同步到MapView组件。
2. 地图初始化：使用MapComponent组件，MapOptions配置mapType为STANDARD。
3. 尝试方案：代码中预留了setMapStylePath()方法调用，但不确定是否需要自定义样式文件。

关键代码位置：MapView.ets中已实现onThemeChanged()方法监听主题变化，当currentColorMode变化时尝试调用setMapStylePath()切换样式，但不确定样式文件格式和路径规则。

咨询问题：
1. Map Kit是否支持系统深浅色模式自动切换？是否有内置深色样式？
2. 如需使用setMapStylePath()方法，样式文件格式是什么？路径规则（如common/map_dark.json）是否正确？
3. 是否有更简单的API实现自动切换？
4. setMapStylePath()在API 20是否可用？是否支持API 17+设备？

期望方案：希望使用最简单的方式实现地图深浅色模式切换，切换时不应有明显延迟，需支持API 17+设备。

Map Kit API Key: 1783644396122594176（配置在module.json5的metadata.client_id中）

